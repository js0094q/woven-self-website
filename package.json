{
  "name": "woven-self-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/embed-form-secret.js || true"
  },
  "dependencies": {
    "@vercel/postgres": "^0.2.0",
    "@sendgrid/mail": "^7.8.0"
  }
}
